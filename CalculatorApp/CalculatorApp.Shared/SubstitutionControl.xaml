<UserControl
    x:Class="CalculatorApp.SubstitutionControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CalculatorApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignWidth="400" Height="79" Margin="0,5,0,0">
    
    <Grid>
    	<Grid.ColumnDefinitions>
    		<ColumnDefinition/>
    		<ColumnDefinition/>
    	</Grid.ColumnDefinitions>
    	<TextBox TextWrapping="Wrap" VerticalAlignment="Center" Margin="15,0" Header="from" Text="{Binding Key, Mode=TwoWay}"/>
    	<TextBox TextWrapping="Wrap" VerticalAlignment="Center" Grid.Column="1" Margin="15,0" Header="to" Text="{Binding Value, Mode=TwoWay}"/>

    </Grid>
</UserControl>
